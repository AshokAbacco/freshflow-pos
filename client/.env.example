# Leave empty to call the API on the same origin (/api), which is how the Vite proxy and nginx are set up.
VITE_API_URL=
# Where `npm run dev` proxies /api requests
VITE_DEV_API_TARGET=http://localhost:4000
